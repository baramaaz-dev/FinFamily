# FinFamily — استراتيجية المشروع

---

## Epics

- **E1** — البنية التحتية والإعداد
- **E2** — إدارة الأشخاص والشركاء
- **E3** — المحافظ المالية والمشاريع
- **E4** — العقارات والإيجارات
- **E5** — المعاملات المالية
- **E6** — حسابات رأس المال والتسويات
- **E7** — لوحة التحكم الرئيسية
- **E8** — التقارير والتصدير
- **E9** — الإعدادات والأسعار
- **E10** — الشركة العائلية والكيان الجامع

---

## Sprint 0 — الإعداد والبنية التحتية

### E1 — البنية التحتية والإعداد

- `S-001` إعداد بيئة التطوير والأدوات الخارجية
  - إنشاء مشروع Vite + React + TypeScript
  - إعداد Tailwind CSS + Shadcn/UI
  - إعداد مشروع Supabase (Auth + Database)
  - تفعيل RLS على قاعدة البيانات
  - إنشاء ملف `.env` وتهيئة متغيرات البيئة
  - ربط المشروع بـ GitHub وإعداد branch strategy
  - نشر المشروع الأولي على Vercel
- `S-002` إعداد React Router DOM وهيكل الصفحات
- `S-003` بناء Layout الرئيسي RTL (Sidebar + Header + Content)
- `S-004` إعداد نظام المصادقة (Login Page + Protected Routes)
- `S-005` تهيئة Dinero.js ومنطق العملات (USD/SYP)

---

## Sprint 1 — قاعدة البيانات والأشخاص

### E1 — البنية التحتية والإعداد

- `S-006` تطبيق migrations لجميع جداول قاعدة البيانات
- `S-007` كتابة RLS policies لجميع الجداول
- `S-008` إنشاء seed data للتجربة

### E2 — إدارة الأشخاص والشركاء

- `S-015` صفحة قائمة الأشخاص (people)
- `S-016` نموذج إضافة شخص جديد
- `S-017` نموذج تعديل بيانات شخص
- `S-018` حذف شخص (مع التحقق من عدم ارتباطه بأصول)

---

## Sprint 2 — المحافظ المالية

### E3 — المحافظ المالية والمشاريع

- `S-019` صفحة قائمة المحافظ
- `S-020` نموذج إنشاء محفظة جديدة (مع اختيار النوع)
- `S-021` نموذج تعديل محفظة
- `S-022` إضافة مساهمين للمحفظة (portfolio_members)
- `S-023` نموذج تحديد حصص المساهمين بالكسور
- `S-024` التحقق من أن مجموع الحصص = 1 قبل الحفظ
- `S-025` عرض تفاصيل المحفظة (رصيد + مساهمون + حصص)

---

## Sprint 3 — المعاملات المالية

### E5 — المعاملات المالية

- `S-026` صفحة سجل المعاملات
- `S-027` نموذج تسجيل معاملة جديدة (دخل/مصروف/تحويل)
- `S-028` اختيار العملة وإدخال سعر الصرف
- `S-029` ربط المعاملة بمحفظة
- `S-030` فلترة المعاملات (بالنوع / التاريخ / المحفظة)
- `S-031` بحث في المعاملات
- `S-032` تعديل وحذف معاملة

---

## Sprint 4 — العقارات

### E4 — العقارات والإيجارات

- `S-033` صفحة قائمة العقارات
- `S-034` نموذج إضافة عقار جديد
- `S-035` إضافة ملاك العقار مع الحصص وعلة التملك
- `S-036` التحقق من مجموع حصص الملاك = 1
- `S-037` عرض بيان ملكية العقار
- `S-038` نموذج تسجيل عقد إيجار جديد
- `S-039` سجل دفعات الإيجار
- `S-040` تسجيل دفعة إيجار جديدة
- `S-041` نموذج تسجيل مصروف عقار
- `S-042` سجل مصروفات العقار مع الفلترة
- `S-043` عرض الالتزامات القادمة للعقار

---

## Sprint 5 — أسعار الصرف والإعدادات

### E9 — الإعدادات والأسعار

- `S-044` صفحة إدارة أسعار الصرف
- `S-045` نموذج إضافة سعر صرف جديد (SYP مقابل USD)
- `S-046` عرض سجل أسعار الصرف
- `S-047` جلب آخر سعر صرف تلقائياً في النماذج

---

## Sprint 6 — حسابات رأس المال

### E9 — الإعدادات والأسعار

- `S-083` صفحة دليل الحسابات (IFRS 18) — شرط مرجعي لقصص E6

### E6 — حسابات رأس المال والتسويات

- `S-048` إنشاء حساب رأس مال لشريك/كيان
- `S-049` تسجيل معاملة رأسمالية (ضخ/سحب/ربح/خسارة)
- `S-050` عرض كشف حساب رأسمالي لشريك
- `S-051` حساب رأس المال الختامي وفق المعادلة
- `S-052` نموذج إنشاء تسوية أرباح جديدة (profit_settlement)
- `S-053` احتساب حصص الشركاء في التسوية تلقائياً
- `S-054` تأكيد التسوية وتغيير الحالة إلى confirmed
- `S-055` ربط settlement_shares بـ capital_transactions

---

## Sprint 7 — صفحة الشركاء

### E2 — إدارة الأشخاص والشركاء

- `S-056` صفحة تفاصيل شريك (جميع كياناته)
- `S-057` عرض حصة الشريك في كل محفظة وعقار
- `S-058` سجل مسحوبات الشريك
- `S-059` نموذج تسجيل مسحوبات جديدة (distributions)
- `S-060` ملخص رأس مال الشريك عبر جميع الكيانات

---

## Sprint 8 — لوحة التحكم

### E7 — لوحة التحكم الرئيسية

- `S-061` بطاقة صافي الثروة الإجمالي (USD)
- `S-062` بطاقات المحافظ المالية مع الأرصدة
- `S-063` قسم الالتزامات القادمة ⚠️ (إيجارات + مصروفات)
- `S-064` جدول آخر 5 معاملات
- `S-065` عرض نصيب كل شريك من الثروة الإجمالية
- `S-066` رسم بياني توزيع الأصول (Recharts)
- `S-067` مؤشر P&L للفترة الحالية

---

## Sprint 9 — التقارير والتصدير

### E8 — التقارير والتصدير

- `S-068` تقرير الأرباح والخسائر P&L (بالفترة)
- `S-069` قائمة حقوق الشركاء
- `S-070` كشف حساب شريك محدد
- `S-071` الميزانية المبسطة (أصول / التزامات / حقوق)
- `S-072` فلترة التقارير بالفترة الزمنية والكيان
- `S-073` تصدير أي تقرير إلى PDF (jsPDF + html2canvas)
- `S-074` تنسيق صفحة PDF للطباعة (RTL)

---

## Sprint 10 — الإتمام والجودة

### E1 — البنية التحتية والإعداد

- `S-075` معالجة الأخطاء الشاملة (Error Boundaries + Toast)
- `S-076` حالات التحميل (Skeleton Loaders)
- `S-077` التحقق من صحة جميع النماذج (Zod schemas)
- `S-078` اختبار RLS policies على جميع الجداول
- `S-079` مراجعة التوافق الكامل مع RTL
- `S-080` تحسين الأداء (React Query caching)
- `S-081` اختبار على الموبايل (Responsive)
- `S-082` مراجعة أمنية نهائية قبل الإنتاج

---

## Sprint 11 — المحرك المحاسبي (القيد المزدوج)

> **المرجع:** STR-006 v1.2 — المرجع الإلزامي لجميع قصص هذا الـ Sprint.
> **الهدف:** تفعيل نظام القيد المزدوج وربط جميع الكيانات المالية بالأستاذ العام.
> **الأولوية:** E1 (البنية التحتية) أولاً — يجب إتمام S-084 إلى S-087 قبل البدء بأي قصة أخرى.

### E1 — البنية التحتية والإعداد

- `S-084` Trigger إنشاء حسابات الشركاء تلقائياً
  - إنشاء دالة `auto_create_partner_accounts()` في PostgreSQL
  - ربطها بـ `AFTER INSERT ON people`
  - Migration لإنشاء حسابات `31XX / 32XX` لجميع الأشخاص الحاليين في قاعدة البيانات
  - التحقق من عدم تكرار الحسابات (idempotent)

- `S-085` RPC ترحيل القيد المفرد — `post_journal_entry`
  - دالة PostgreSQL تُنفِّذ الخطوات السبع من STR-006 §10.1 في transaction واحدة
  - دعم جميع أنواع المصادر: `transaction · lease_payment · property_expense · capital_transaction`
  - تطبيق قوالب القيود من STR-006 §5 حسب `source_type`
  - منع الترحيل المزدوج + التحقق من التوازن + التحقق من فتح الفترة

- `S-086` RPC ترحيل قيد التسوية المُركَّب — `post_settlement_entry`
  - دالة PostgreSQL خاصة بـ `profit_settlement` (STR-006 §8.4)
  - قيد واحد بـ N+1 سطر: مدين واحد للإيرادات الكلية + دائن لكل شريك
  - إضافة عمود `journal_entry_id` إلى جدول `profit_settlements`
  - التحقق من: Σ partnerAmount = total_profit قبل الترحيل

- `S-087` RPC حذف حسابات الشريك — `delete_partner_accounts`
  - دالة PostgreSQL تتحقق من `journal_entry_lines` قبل الحذف
  - رمي استثناء `ACCOUNTS_HAVE_ENTRIES` عند وجود قيود مرتبطة
  - تكامل مع زر "حذف الشريك" في صفحة إدارة الأشخاص

### E5 — المعاملات المالية

- `S-088` ربط ترحيل القيود بالمعاملات المالية
  - إنشاء Hook موحَّد `usePostJournalEntry()`
  - استدعاء `post_journal_entry` تلقائياً في `onSubmit` نموذج إضافة معاملة
  - `invalidateQueries` لمفاتيح: `['transactions'] · ['journal-entries'] · ['dashboard']`

### E4 — العقارات والإيجارات

- `S-089` ربط ترحيل القيود بدفعات الإيجار ومصروفات العقار
  - استدعاء `post_journal_entry` في `onSubmit` نموذج تسجيل دفعة إيجار
  - استدعاء `post_journal_entry` في `onSubmit` نموذج تسجيل مصروف عقار
  - `invalidateQueries` لمفاتيح: `['lease-payments', leaseId] · ['property-expenses', propertyId] · ['dashboard']`

### E6 — حسابات رأس المال والتسويات

- `S-090` ربط ترحيل القيود بالحركات الرأسمالية
  - استدعاء `post_journal_entry` في `onSubmit` نموذج تسجيل حركة رأسمالية
  - `invalidateQueries` لمفاتيح: `['capital-transactions', accountId] · ['capital-accounts'] · ['dashboard']`

- `S-091` تحديث تأكيد التسوية للقيد المُركَّب
  - استبدال منطق N قيود منفصلة بـ `post_settlement_entry` (قيد واحد N+1 سطر)
  - `invalidateQueries` لكل `['capital-accounts', partnerId]` في التسوية
  - `invalidateQueries` لمفاتيح: `['profit-settlements'] · ['journal-entries'] · ['dashboard']`

### E1 — البنية التحتية والإعداد

- `S-092` مؤشرات حالة الترحيل في الواجهة
  - شارة "مُرحَّل / غير مُرحَّل" مبنية على `journal_entry_id IS NOT NULL`
  - تطبيق موحَّد عبر: سجل المعاملات · سجل دفعات الإيجار · سجل مصروفات العقار · كشف الحركات الرأسمالية
  - رسالة خطأ واضحة عند فشل الترحيل مع إمكانية إعادة المحاولة

---

## Sprint 12 — الكيان الجامع والبنية التحتية المحاسبية

> **الهدف:** إنشاء الشركة العائلية الافتراضية كمظلة جامعة لجميع الكيانات والأصول،
> وبناء البنية التحتية اللازمة لدورة العمل المحاسبية الكاملة.
> **الأولوية:** E10 (الكيان الجامع) أولاً — يجب إتمام S-093 إلى S-095 قبل S-096 و S-097.

### E10 — الشركة العائلية والكيان الجامع

- `S-093` إنشاء كيان الشركة العائلية الافتراضية
  - جدول `company` يحمل اسم الشركة وتاريخ التأسيس والعملة المرجعية والملاحظات
  - إعداد واحد فقط (single-row) — الشركة ليست قائمة متعددة
  - صفحة إعدادات الشركة ضمن قسم الإعدادات
  - RLS وتطبيق migrations

- `S-094` تحديد حصص الشركاء في الكيان الجامع
  - جدول `company_members` يربط الشركة بالشركاء مع حصصهم الإجمالية (بسط/مقام)
  - التحقق من مجموع الحصص = 1 قبل الحفظ
  - هذه الحصص تمثل نصيب كل وارث من الشركة ككل (بمعزل عن حصصه في كل كيان)
  - واجهة إدارة أعضاء الشركة

- `S-095` ربط شجرة الحسابات بالكيان الجامع
  - إضافة عمود `company_id` إلى جدول `accounts`
  - جميع الحسابات تنتمي للشركة الافتراضية
  - تحديث `general_ledger` VIEW ليعكس انتماء الحسابات للشركة
  - Migration لتحديث السجلات الحالية

- `S-096` إدخال الأرصدة الافتتاحية التاريخية
  - واجهة إدخال رصيد افتتاحي لكل حساب في شجرة الحسابات
  - قيود افتتاحية من نوع `source_type = 'opening_balance'` تُرحَّل في أول فترة
  - ربط البيانات التاريخية (قبل Sprint 11) بالأستاذ العام
  - التحقق من توازن الأرصدة الافتتاحية (Σ أصول = Σ خصوم + حقوق)

### E5 — المعاملات المالية

- `S-097` واجهة دفتر اليومية العام
  - نموذج إدخال قيود يدوية (`source_type = 'manual'`) لا ترتبط بمحفظة أو عقار
  - دعم القيود المركبة: إضافة سطور مدين/دائن متعددة ديناميكياً
  - التحقق من التوازن: Σ مدين = Σ دائن قبل الحفظ
  - حفظ القيد بحالة `draft` (لا ترحيل تلقائي — ينتظر مراجعة Sprint 13)
  - صفحة سجل القيود اليدوية مع فلترة بالتاريخ والحساب

---

## Sprint 13 — دورة المراجعة والقوائم المالية

> **الهدف:** تحويل دورة العمل المحاسبية من ترحيل آلي فوري إلى مراجعة يدوية بصلاحيات
> منفصلة (محاسب يُدخل · مدير مالي يُرحِّل)، وإعادة بناء التقارير من الأستاذ العام.
> **ملاحظة هامة:** هذا الـ Sprint يُعدِّل فلسفة الترحيل الحالية من Sprint 11 —
> القيود ستُحفظ كـ `draft` عند الإدخال بدلاً من الترحيل الفوري.

### E1 — البنية التحتية والإعداد

- `S-098` تحويل الترحيل من آلي إلى يدوي
  - تعديل `usePostJournalEntry()` لحفظ القيد كـ `draft` بدلاً من `posted`
  - تعديل `post_journal_entry` RPC: إزالة خطوة UPDATE status → 'posted' من التسلسل التلقائي
  - تعديل `post_settlement_entry` RPC بالمنطق ذاته
  - شارة "معلّق" تحل محل شارة "غير مُرحَّل" للقيود الجديدة

- `S-099` صفحة مراجعة القيود المعلقة
  - صفحة `/journal-review` تجمع جميع قيود `status = 'draft'` من جميع المصادر
  - عرض: رقم القيد · التاريخ · المصدر · الحسابات المدينة/الدائنة · المبلغ
  - فلترة بالتاريخ · المصدر · الحساب
  - مؤشر التوازن لكل قيد (Σ مدين = Σ دائن)

- `S-100` واجهة ترحيل القيود (صلاحية المدير المالي)
  - زر "ترحيل" لكل قيد منفرد
  - زر "ترحيل الكل" للقيود المحددة (bulk posting)
  - تأكيد قبل كل عملية ترحيل
  - القيد المُرحَّل يختفي من قائمة المعلقة ويظهر في الأستاذ العام

- `S-101` القيد العكسي مع سبب إلزامي
  - زر "قيد عكسي" على كل قيد مُرحَّل في صفحة المراجعة والأستاذ العام
  - Dialog يستلزم إدخال سبب العكس (حقل نصي إلزامي)
  - إنشاء قيد عكسي آلي مرتبط بالأصلي مع حفظ السبب في `description`
  - تحديث القيد الأصلي إلى `status = 'reversed'`

### E8 — التقارير والتصدير

- `S-102` صفحة ميزان المراجعة
  - جدول يعرض كل حساب مع إجمالي مدينه ودائنه ورصيده في أي تاريخ
  - مصدر البيانات: `general_ledger` VIEW (القيود المُرحَّلة فقط)
  - فلترة بالفترة المحاسبية والتاريخ
  - تحقق تلقائي: Σ أرصدة مدينة = Σ أرصدة دائنة
  - تصدير PDF

### E9 — الإعدادات والأسعار

- `S-103` إقفال الفترة المحاسبية
  - زر إقفال الفترة مع تأكيد وتحقق من عدم وجود قيود معلقة
  - قيود إقفال آلية تُرحِّل أرصدة الإيرادات والمصروفات إلى حساب الأرباح المحتجزة (3300)
  - تغيير حالة الفترة: open → closed → locked
  - منع أي ترحيل جديد على الفترة المُقفَلة نهائياً

### E8 — التقارير والتصدير

- `S-104` إعادة بناء التقارير من الأستاذ العام
  - إعادة كتابة S-068 (P&L) لتستمد بياناتها من `journal_entry_lines` عبر `general_ledger`
  - إعادة كتابة S-069 (حقوق الشركاء) من الأستاذ العام
  - إعادة كتابة S-071 (الميزانية) من الأستاذ العام
  - الأرقام الآن تعكس القيود المُرحَّلة فعلاً لا الجداول المصدرية
  - دعم الفترات المحاسبية المُقفَلة في التقارير التاريخية